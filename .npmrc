# vue-select@3.x declara peer vue@2.x pero el proyecto usa Vue 3.
# legacy-peer-deps evita que npm (v7+) aborte por ese conflicto de peers,
# SIN cambiar ninguna versión instalada. Permite que `npm install`/`npm ci`
# funcionen en dev y producción sin flags.
legacy-peer-deps=true
